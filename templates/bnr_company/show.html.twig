{% extends 'base.html.twig' %}

{% block title %}BnrCompany{% endblock %}

{% block body %}
    <h1>BnrCompany</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Нэр</th>
                <td>{{ bnr_company.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bnr_company_index') }}">back to list</a>
    <br>

    <a href="{{ path('bnr_company_edit', {'id': bnr_company.id}) }}">edit</a>

    {{ include('bnr_company/_delete_form.html.twig') }}
{% endblock %}
