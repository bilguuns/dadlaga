<form method="post" action="{{ path('bnr_banner_delete', {'id': bnr_banner.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bnr_banner.id) }}">
    <button class="btn">Delete</button>
</form>
