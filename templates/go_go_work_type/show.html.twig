{% extends 'base.html.twig' %}

{% block title %}GoGoWorkType{% endblock %}

{% block body %}
    <h1>GoGoWorkType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ go_go_work_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ go_go_work_type.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ go_go_work_type.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('go_go_work_type_index') }}">back to list</a>

    <a href="{{ path('go_go_work_type_edit', {'id': go_go_work_type.id}) }}">edit</a>

    {{ include('go_go_work_type/_delete_form.html.twig') }}
{% endblock %}
