<form method="post" action="{{ path('go_go_work_type_delete', {'id': go_go_work_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ go_go_work_type.id) }}">
    <button class="btn">Delete</button>
</form>
