{% extends 'base.html.twig' %}

{% block title %}GoGoWorkType index{% endblock %}

{% block body %}
    <h1>GoGoWorkType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Price</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for go_go_work_type in go_go_work_types %}
            <tr>
                <td>{{ go_go_work_type.id }}</td>
                <td>{{ go_go_work_type.name }}</td>
                <td>{{ go_go_work_type.price }}</td>
                <td>
                    <a href="{{ path('go_go_work_type_show', {'id': go_go_work_type.id}) }}">show</a>
                    <a href="{{ path('go_go_work_type_edit', {'id': go_go_work_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('go_go_work_type_new') }}">Create new</a>
{% endblock %}
