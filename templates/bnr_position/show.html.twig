{% extends 'base.html.twig' %}

{% block title %}BnrPosition{% endblock %}

{% block body %}
    <h1>BnrPosition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bnr_position.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ bnr_position.name }}</td>
            </tr>
            <tr>
                <th>Width</th>
                <td>{{ bnr_position.width }}</td>
            </tr>
            <tr>
                <th>Height</th>
                <td>{{ bnr_position.height }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ bnr_position.picture }}</td>
            </tr>
            <tr>
                <th>HtmlID</th>
                <td>{{ bnr_position.htmlID }}</td>
            </tr>
            <tr>
                <th>DefaultBanner</th>
                <td>{{ bnr_position.defaultBanner }}</td>
            </tr>
            <tr>
                <th>Service_id</th>
                <td>{{ bnr_position.serviceId }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ bnr_position.price }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ bnr_position.title }}</td>
            </tr>
            <tr>
                <th>Ctitle</th>
                <td>{{ bnr_position.ctitle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bnr_position_index') }}">back to list</a>

    <a href="{{ path('bnr_position_edit', {'id': bnr_position.id}) }}">edit</a>

    {{ include('bnr_position/_delete_form.html.twig') }}
{% endblock %}
